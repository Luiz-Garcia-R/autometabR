% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autometabR-package.R
\docType{package}
\name{autometabR}
\alias{autometabR-package}
\alias{autometabR}
\title{autometabR: Quick Reference and Main Functions for Metabolomics Analysis}
\description{
autometabR provides a comprehensive set of functions for metabolomics data analysis,
from raw data import to normalization, quality control, exploratory and differential
analysis. This help topic serves as a quick reference and guide for users.
}
\details{
Main Functions Overview\tabular{ll}{
   Function \tab Description \cr
   \code{metabol.import()} \tab Import and validate metabolomics data \cr
   \code{metabol.normalize()} \tab Normalize, filter, and impute missing values \cr
   \code{metabol.qc()} \tab QC plots: boxplots, PCA, density distributions \cr
   \code{metabol.info()} \tab Summarize metabolites and group statistics \cr
   \code{metabol.corr()} \tab Sample/group correlation matrices \cr
   \code{metabol.dems()} \tab Identify differentially expressed metabolites \cr
   \code{metabol.dimred()} \tab PCA and UMAP for dimensionality reduction \cr
   \code{metabol.enrich()} \tab Metabolite set enrichment analysis \cr
   \code{metabol.heatmap()} \tab Heatmap of top variable metabolites \cr
   \code{metabol.oplsda()} \tab Group separation via OPLS-DA \cr
   \code{metabol.roc()} \tab ROC curves for discriminant metabolites \cr
   \code{metabol.ttest()} \tab T-test or Mann-Whitney test \cr
   \code{metabol.venn()} \tab Overlap visualization with Venn diagrams \cr
   \code{metabol.vip()} \tab Highlight top contributing metabolites \cr
}


Contact and Contributions
For suggestions, bug reports, or contributions, see the
\href{https://github.com/Luiz-Garcia-R/autometabR}{GitHub repository}.

Example Workflow

A small synthetic metabolomics dataset with 50 metabolites and 5 samples per group.
This dataset is for demonstration purposes only.
}
\examples{
\dontrun{

# Small synthetic metabolomics dataset
metabolites <- paste0("M", sprintf("\%03d", 1:50))
raw_data <- data.frame(
  Sample = paste0("S", 1:10),
  matrix(
    rnorm(50*10, mean = 100, sd = 20),
    nrow = 10,
    ncol = 50,
    dimnames = list(NULL, metabolites)
  )
)

metadata <- data.frame(
  Sample = raw_data$Sample,
  Group  = rep(c("Control","Treatment"), each = 5)
)

# Convert to metabolR object
imp_data <- metabol.import(raw_data, metadata)

# Normalize
normalized <- metabol.normalize(imp_data)

# QC
metabol.qc(normalized, metadata)

# Exploratory and differential analysis
metabol.corr(normalized, metadata)
metabol.dems(normalized, metadata)

# Print summary of normalization
print(normalized)
}

}
\author{
\strong{Maintainer}: Luiz Garcia \email{luiz.cardoso@ufpr.br} (\href{https://orcid.org/0000-0002-9616-0927}{ORCID}) (UFPR)

}
