% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MetabolVip.R
\name{metabol.vip}
\alias{metabol.vip}
\title{VIP Scores and Heatmap for Dimensionality Reduction Results}
\usage{
metabol.vip(
  normalized_data,
  top_n = 20,
  component = 1,
  plot = TRUE,
  calc_auc = FALSE,
  group_col = "Group",
  assign_result = TRUE,
  assign_name = "vip_data",
  envir = parent.frame()
)
}
\arguments{
\item{normalized_data}{Object from metabol.dimred().}

\item{top_n}{Integer, number of top metabolites to display. Default = 20.}

\item{component}{Integer, which component to extract VIP scores from. Default = 1.}

\item{plot}{Logical, whether to generate a bar plot. Default = TRUE.}

\item{calc_auc}{Logical, whether to compute AUC for classification performance. Default = FALSE.}

\item{group_col}{Character, metadata column indicating sample groups. Default = "Group".}

\item{assign_result}{Logical, whether to assign the result to the environment. Default = TRUE.}

\item{assign_name}{Character, name of the object if assigned. Default = "vip_data".}

\item{envir}{Environment where the object will be assigned. Default = parent.frame().}
}
\value{
A data frame of VIP scores, optionally plotted or assigned to the environment.
}
\description{
Computes Variable Importance in Projection (VIP) scores from a PLS-DA
object contained in a dimensionality reduction result, plots top VIPs and
a heatmap across groups, and optionally calculates AUC for each variable.
}
\examples{
\dontrun{
# Minimal example
set.seed(123)

expr_mat <- data.frame(
  Met1 = rnorm(6),
  Met2 = rnorm(6),
  Met3 = rnorm(6)
)
metadata <- data.frame(
  Sample = paste0("S", 1:6),
  Group  = rep(c("A","B"), each = 3)
)

plsda_res <- mixOmics::plsda(expr_mat, metadata$Group, ncomp = 2)

dimred_data <- list(
  expr_matrix = expr_mat,
  metadata    = metadata,
  res_dimred  = list(plsda_res = plsda_res)
)

metabol.vip(dimred_data, n_top = 3, component = 1, plot = TRUE)
}

}
\references{
Wold, S., Sjöström, M., & Eriksson, L. (2001). PLS-regression: a basic tool of chemometrics. \emph{Chemometrics and Intelligent Laboratory Systems}, 58(2), 109–130. \url{doi:10.1016/S0169-7439(01)00155-1}
Chong, I.G., & Jun, C.H. (2005). Performance of some variable selection methods when multicollinearity is present. \emph{Chemometrics and Intelligent Laboratory Systems}, 78(1–2), 103–112. \url{doi:10.1016/j.chemolab.2005.04.003}
}
