% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MetabolBoxplot.R
\name{metabol.boxplot}
\alias{metabol.boxplot}
\title{Boxplot/Violin Plot for a Single Metabolite with Limma Significance}
\usage{
metabol.boxplot(
  normalized_data,
  metadata = NULL,
  metabolite,
  group_col = "Group",
  limma_results = NULL
)
}
\arguments{
\item{normalized_data}{A list containing at least \code{expr_matrix}, and optionally
\code{metadata} and \code{limma_results}.}

\item{metadata}{Optional data frame with sample metadata. Must contain a \code{"Sample"}
column and the grouping column specified in \code{group_col}.}

\item{metabolite}{Character string indicating the metabolite to plot.
Must match a column name in \code{expr_matrix}.}

\item{group_col}{Character string indicating the grouping variable in metadata.
Default is \code{"Group"}.}

\item{limma_results}{Optional list containing a \code{results} data frame with
\code{Metabolite} and \code{P.Value} columns. If omitted, the function will try to
use \code{normalized_data$limma_results}.}
}
\value{
A \code{ggplot2} object containing a violin + boxplot with significance annotation.
}
\description{
Generates a violin + boxplot visualization for a selected metabolite across
two experimental groups. The function integrates expression values,
metadata, and \code{limma} differential analysis results to annotate the plot
with significance levels (*, **, ***, ns).
}
\details{
This function expects \code{normalized_data} to follow the structure produced by
your metabolomics workflow, containing at minimum:
\itemize{
\item \code{expr_matrix}: a matrix or data frame with a column \code{"Sample"} and one
column per metabolite.
\item \code{metadata} (optional): a data frame with sample-level metadata, including
a group column (default \code{"Group"}).
\item \code{limma_results} (optional): a list containing a data frame \code{results}
with columns \code{"Metabolite"} and \code{"P.Value"}.
}

If metadata or limma results are not supplied directly, the function tries to
extract them from \code{normalized_data}.

The function supports \strong{exactly two groups} for plotting and significance
annotation.
}
\examples{
\dontrun{
metabol.boxplot(
  normalized_data = my_norm,
  metabolite = "Lactate",
  group_col = "Condition"
)
}

}
