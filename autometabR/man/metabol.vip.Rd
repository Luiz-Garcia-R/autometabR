% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MetabolVip.R
\name{metabol.vip}
\alias{metabol.vip}
\title{VIP Scores and Heatmap for Dimensionality Reduction Results}
\usage{
metabol.vip(
  dimred_obj,
  n_top = 20,
  component = 1,
  plot = TRUE,
  calc_auc = FALSE,
  group_col = "Group"
)
}
\arguments{
\item{dimred_obj}{List returned by \code{metabol.dimred()} containing \code{expr_matrix}, \code{metadata}, and \code{res_dimred$plsda_res}.}

\item{n_top}{Number of top VIP variables to show (default = 20).}

\item{component}{Component to extract VIP scores from (default = 1).}

\item{plot}{Logical, whether to plot VIP and heatmap (default = TRUE).}

\item{calc_auc}{Logical, whether to calculate AUC for each VIP variable (default = FALSE).}

\item{group_col}{Name of column in metadata defining groups (default = "Group").}
}
\value{
Invisibly returns a list containing VIP scores, top VIPs, AUC (if requested), and plot objects.
}
\description{
Computes Variable Importance in Projection (VIP) scores from a PLS-DA
object contained in a dimensionality reduction result, plots top VIPs and
a heatmap across groups, and optionally calculates AUC for each variable.
}
\examples{
\dontrun{
# Minimal example
set.seed(123)

expr_mat <- data.frame(
  Met1 = rnorm(6),
  Met2 = rnorm(6),
  Met3 = rnorm(6)
)
metadata <- data.frame(
  Sample = paste0("S", 1:6),
  Group  = rep(c("A","B"), each = 3)
)

plsda_res <- mixOmics::plsda(expr_mat, metadata$Group, ncomp = 2)

dimred_obj <- list(
  expr_matrix = expr_mat,
  metadata    = metadata,
  res_dimred  = list(plsda_res = plsda_res)
)

metabol.vip(dimred_obj, n_top = 3, component = 1, plot = TRUE)
}

}
\references{
Wold, S., Sjöström, M., & Eriksson, L. (2001). PLS-regression: a basic tool of chemometrics. \emph{Chemometrics and Intelligent Laboratory Systems}, 58(2), 109–130. \url{doi:10.1016/S0169-7439(01)00155-1}
Chong, I.G., & Jun, C.H. (2005). Performance of some variable selection methods when multicollinearity is present. \emph{Chemometrics and Intelligent Laboratory Systems}, 78(1–2), 103–112. \url{doi:10.1016/j.chemolab.2005.04.003}
}
